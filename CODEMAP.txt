# OrderHelper 코드맵
# 의도 기준: APP_INTENT.md (BBQ 재고 입력과 발주량 계산 흐름 보호)

## 파일 → 역할
- APP_INTENT.md: 앱 목적, 사용자 결과, 절대 기준, Web+SiteBot 경계, read-only preflight, 공장 PC browser evidence, live write boundary, 완료 기준
- index.html: 입력+출력 단일 9열 표, 구역 입력순↔최초 SFA순 정렬 컨텍스트, stable `itemKey`/구역별 `entryKey`, keyed 계산 셀 즉시 patch와 focus/caret/stock-flow epoch fence, `inventoryByItemKey`+latest revision current/history read model, Enter/명시 행동만 확정하는 immutable `bbq_confirmed_save_queue_v1`, 행내 사이트/별명/신규품목/미매칭 처리, lossless `sfaOrderLedger` v2, PC/SiteBot용 `aiUsageAnalysis.evidence`와 advisory-only UI, 수동 하루사용량/환산 보호, Firebase 동기화, 인증 게이트. 환산 방향은 `발주 1단위 = 체크/재고 N단위`다.
- tests/orderhelper_static_checks.js: 기존 전체 정적/VM 회귀 + stable key/revision/명시 alias 생성 계약 검증
- tests/orderhelper_inventory_matching_regression.js: 입력 재고→출력 합계→current/history keyed payload, duplicate zone/id, legacy name, 0/blank, active+queued Enter revision fence, 신규품목/별명/동명이름/빈 배열 회귀
- tests/orderhelper_autosave_regression.js + tests/orderhelper_autosave_browser.py: draft-only PUT 0, Enter current/history exact body, active+queued, timeout/부분실패 exact retry, reload 복구, quota/corruption fail-closed 회귀
- tests/orderhelper_single_grid_ledger_regression.js: 단일 표 정렬 2종, keyed focus/caret, 150행 입력 burst scheduler/render count, same-run retry/new-run append/duplicate/0/remote merge/누적 total, AI evidence 수동값 보호 회귀
- /order/desk_q7m9r3a8: read-only preflight source. records/state 확인용 Firebase 경로.
- ORDER_LIST_ANALYSIS_HANDOFF.md: 발주 자동화 조사/진단 인계
- /root/SiteBot/main_auto.pyw: PC 다운로드/SFA 분석 실행자. `/monitor/main_pc/sfa_order_request`를 읽고 `/monitor/main_pc/status.sfa_order` 상태를 올린다.
- /sitebot/heartbeat/main_pc: OrderHelper의 SiteBot read-only preflight용 heartbeat evidence.
- /root/StoreBot/bot/order_excel_analyzer.py: SFA 엑셀 parser/compare 공용 경로. `SFA_ITEM_MASTER_ALIASES`는 SFA 원문 품목명 → OrderHelper MASTER 명시 매핑이며, 전역 threshold를 낮추지 않는다. g/벌크/배달용/내점용 같은 규격·용도 표기는 오매칭 방지 신호로 본다.
- /root/my-first-project/scripts/orderhelper_usage_inference.py: 재고 history + SFA 실발주 엑셀/비교 이력으로 `이전재고+입고-다음재고` 실사용량과 발주단위 환산을 추정해 `/unitInference/latest`에 올린다. 실사용 분석은 참고용이고, `/sfaActualHistory` 백필은 `--backfill-sfa-actual-history` 또는 `--bulk-backfill-local-sfa-actual-history` 명시 플래그가 필요하며, 수동 하루사용량 자동 write는 금지한다.
- /root/my-first-project/scripts/ai_ops_common_monitor.py: 서버폰 Termux 상주 감시자. OrderHelper 요청/상태/PC heartbeat 정체와 `current`/`sfaCompare/latest`/`sfaActualHistory`/`unitInference/latest`/최근 `history`의 실발주 이력·단위 환산·실사용량 분석 누락 신호를 본다. main_pc heartbeat는 20분 초과~45분까지 warn, 45분 초과부터 error/self_fix다.

## 수정 시
- 코드 변경 전 APP_INTENT.md를 먼저 확인한다.
- 입력 동선, 계산식, SFA 대응, Firebase 경계가 바뀌면 APP_INTENT.md와 이 CODEMAP을 같이 갱신한다.
- 웹 UI 변경은 Playwright desktop/mobile screenshot, DOM smoke, Axe, empty state, 공장 PC/SiteBot evidence로 확인한다.
- 외부 사이트 live 조작/발주/주문 write는 read-only preflight와 분리된 별도 safety flow 없이는 진행하지 않는다.
- 라이브 반영 확인: `https://wk7007-wk.github.io/OrderHelper/` 에서 `APP_VERSION` 문자열 확인.
- Termux 감시만 바뀐 경우 OrderHelper 웹 배포는 필요 없다. PC/SiteBot 실행 경계와 Firebase 상태 경로만 검증한다.
- CLI 감시는 누락/정체/불일치 보고, `/unitInference/latest` 리포트 업로드, 코드/문서/배포 보정 역할이다. 엑셀 실발주 이력은 참고 배지/툴팁에 즉시 반영하지만, 발주 품목, 단위, 하루사용량 수동값, 환산, SFA 실발주 원본은 자동 확정하지 않는다.
- `orderSiteMappings`, `orderAliasMappings`, `orderUnitCorrections`, `orderManualItems`는 원천 SFA 행을 바꾸지 않는 사용자 보정 상태다. 로컬 저장소와 `/current`/`history` payload에 분리 저장하고 엑셀분석 요청 payload에도 포함한다. `orderManualItems`는 MASTER에 없는 사이트/SFA 항목을 원명 또는 임의명으로 추가한 내부 target 목록이다. `orderAliasMappingDrafts`는 후보/default 상태 read model이고, `effectiveOrderAliasMappings`는 `confirmed/manual` 우선 후 default를 분석 입력으로 쓰는 read model이다.
- 엑셀/SFA 누적 포인터: local `bbq_sfa_analysis_history`, current/history `sfaOrderLedger`, `/sfaActualHistory` row를 ledger v2로 합친다. stable event id는 run+source row/raw identity를 기준으로 하며 0/duplicate/alias/actual qty/amount를 보존한다. 같은 run retry는 event merge, 다른 run은 append한다.
- AI 실사용 포인터: 브라우저는 모델을 호출하지 않는다. `buildAiUsageEvidence()`가 90일/2000 event bounded ledger+totals+raw ids+수동값 보호 정보를 기존 SFA request의 `aiUsageAnalysis`에 싣고, worker 결과 `aiUsageAdvisory`만 참고 배지로 표시한다. 실제 SiteBot model worker 소비는 별도 작업이다.
- 별명 보정 포인터: `orderAliasMappings`로 사용자사이트 별명→실발주항목+체크단위→발주단위 환산 검수 탭과 체크 입력 행 inline 보정을 공유한다. 최고 점수 후보는 default로 미리 쓰고, 사용자가 후보 선택 시 `confirmed`, 직접 숫자 입력 시 `manual`로 저장한다. payload의 `orderUnitToStockFactor`는 발주량에 곱해 체크/재고 단위로 바꾸는 품목별 값이다.
- 엑셀 결과 수신 포인터: `loadSfaCompareLatest()`는 latest current를 먼저 반영하고 `loadUsageHistory()` 후 `refreshSfaResultViewsWithCurrentState()`를 호출한다. 엑셀 요청 전에는 `flushCurrentBeforeSfaAnalysisRequest()`가 pending 저장을 끝낸 뒤 최신 `buildSfaAnalysisPayloadState()`로 draft/effective payload를 만든다.
- 최신 완료 포인터: `20260714-02 Single grid, lossless order ledger, AI advisory contract`. 검증은 정적/VM 3종 + 로컬 Playwright intercepted DOM smoke + inline syntax + diff-check; 실제 공장 PC/SiteBot worker와 live 화면/배포는 별도다.
- 로컬 SFA 이력 백필 전후 매핑 검증: `orderhelper_usage_inference.py --validate-local-sfa-history`가 `local_sfa_backfill_low_confidence_rows=0`을 요구한다. 2026-06-26 기준 `BBQ충진식패티(100g)(마일드)`와 `BBQ페퍼로니씬피자`는 `두마리치킨,파더스`와 별도 MASTER target이다.
- 보안 게이트: `PIN_HASH`, `AUTH_GEO.storeLat/storeLng=37.2528352/127.4900516`, `radiusM=700`, `allowedDeviceHashes=[]`(CLI 등록 token SHA-256), `/desktopAccess/{grace,devices}` read model, `body.auth-locked .app-shell`. `PIN AND (CLI 허용 단말 OR 승인 desktop token OR 활성 grace desktop OR 서버/호스팅 허용 IP OR GPS)` 구조다. IP 단독 영구 허용 금지, grace는 24시간 만료와 pending/candidate 수집 전용, 승인된 deviceHash만 이후 GPS fallback 통과. `?authDebug=1`은 로컬 개발에서만 GPS 우회. IP 인증/Firebase write 보호는 서버측 Auth/Rules 필요.
